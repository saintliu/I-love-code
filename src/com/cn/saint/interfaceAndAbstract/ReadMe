抽象类：
1. 可以有方法的实现
2. 如果类中有抽象方法，那么这个类一定是抽象类
3. 构造方法不能用static 修饰
4. 抽象类可以实例化，并且一定要实现它的抽象方法
5. 如果实现抽象类，没有实现它的方法，那他本身也应该是一个抽象类
6. 抽象方法实现接口，不一定要实现接口方法

接口：
1. 不能有方法体
2. 不能实现方法
3. 实现接口类，一定要实现其所有的方法
4. 接口之间可以互相继承，而且不一定要实现它的方法
5. 实现类如果要实现接口方法，方法参数、类型、个数、返回值一定要跟接口方法一致